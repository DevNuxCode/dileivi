---
---

<section id="agendar" class="section-padding bg-gradient-to-br from-primary-50 to-white dark:from-gray-800 dark:to-gray-900 transition-colors duration-300">
  <div class="container mx-auto px-4">
    <div class="flex flex-col lg:flex-row gap-12">
      <div class="w-full lg:w-1/2">
        <h2 class="text-3xl md:text-4xl font-bold mb-4 text-gray-900 dark:text-white">Reserva tu Cita</h2>
        <p class="text-lg text-gray-700 dark:text-gray-300 mb-8">
          Agenda una cita con nosotros y disfruta de una experiencia de manicure profesional. Completa el formulario y nos pondremos en contacto contigo.
        </p>
        
        <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-md">
          <form id="booking-form" class="space-y-6">
            <div>
              <label for="name" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Nombre completo</label>
              <input
                type="text"
                id="name"
                name="name"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                placeholder="Tu nombre"
              />
            </div>
            
            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Correo electrónico</label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                placeholder="tucorreo@ejemplo.com"
              />
            </div>
            
            <div>
              <label for="phone" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Número de teléfono</label>
              <input
                type="tel"
                id="phone"
                name="phone"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                placeholder="+56 9 1234 5678"
              />
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
              <div>
                <label for="date" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Fecha</label>
                <input
                  type="date"
                  id="date"
                  name="date"
                  required
                  min={new Date().toISOString().split('T')[0]}
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                />
              </div>
              
              <div>
                <label for="time" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Hora</label>
                <select
                  id="time"
                  name="time"
                  required
                  class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                >
                  <option value="" disabled selected>Selecciona una hora</option>
                  <option value="10:00">10:00 AM</option>
                  <option value="11:00">11:00 AM</option>
                  <option value="12:00">12:00 PM</option>
                  <option value="13:00">1:00 PM</option>
                  <option value="14:00">2:00 PM</option>
                  <option value="15:00">3:00 PM</option>
                  <option value="16:00">4:00 PM</option>
                  <option value="17:00">5:00 PM</option>
                  <option value="18:00">6:00 PM</option>
                </select>
              </div>
            </div>
            
            <div>
              <label for="service" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Servicio</label>
              <select
                id="service"
                name="service"
                required
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
              >
                <option value="" disabled selected>Selecciona un servicio</option>
                <option value="Manicure Tradicional">Manicure Tradicional</option>
                <option value="Manicure Semipermanente">Manicure Semipermanente</option>
                <option value="Uñas Acrílicas">Uñas Acrílicas</option>
                <option value="Nail Art Premium">Nail Art Premium</option>
              </select>
            </div>
            
            <div>
              <label for="message" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">Mensaje (opcional)</label>
              <textarea
                id="message"
                name="message"
                rows="3"
                class="w-full px-4 py-3 rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 text-gray-900 dark:text-white focus:outline-none focus:ring-2 focus:ring-primary-500"
                placeholder="Detalles adicionales sobre tu cita..."
              ></textarea>
            </div>
            
            <button
              type="submit"
              class="w-full py-3 bg-primary-600 hover:bg-primary-700 text-white rounded-lg shadow-md transition-colors duration-300 font-medium"
            >
              Reservar Ahora
            </button>
          </form>
        </div>
      </div>
      
      <div class="w-full lg:w-1/2 flex flex-col justify-center">
        <img 
          src="https://images.pexels.com/photos/3997384/pexels-photo-3997384.jpeg" 
          alt="Manicure Profesional" 
          class="rounded-xl shadow-xl mb-8"
        />
        
        <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-md">
          <h3 class="text-xl font-bold mb-4 text-gray-900 dark:text-white">Horario de Atención</h3>
          <ul class="space-y-3">
            <li class="flex justify-between text-gray-700 dark:text-gray-300">
              <span>Lunes - Viernes:</span>
              <span>10:00 AM - 7:00 PM</span>
            </li>
            <li class="flex justify-between text-gray-700 dark:text-gray-300">
              <span>Sábado:</span>
              <span>10:00 AM - 5:00 PM</span>
            </li>
            <li class="flex justify-between text-gray-700 dark:text-gray-300">
              <span>Domingo:</span>
              <span>Cerrado</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // WhatsApp integration
  const bookingForm = document.getElementById('booking-form');
  
  if (bookingForm) {
    bookingForm.addEventListener('submit', function(e) {
      e.preventDefault();
      
      const name = document.getElementById('name').value;
      const email = document.getElementById('email').value;
      const phone = document.getElementById('phone').value;
      const date = document.getElementById('date').value;
      const time = document.getElementById('time').value;
      const service = document.getElementById('service').value;
      const message = document.getElementById('message').value;
      
      // Format date to a more readable format
      const formattedDate = new Date(date).toLocaleDateString('es-ES', {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      });
      
      // Create WhatsApp message
      const whatsappMessage = `*Nueva Reserva - Dileivi Bello Studio*%0A%0A*Nombre:* ${name}%0A*Email:* ${email}%0A*Teléfono:* ${phone}%0A*Fecha:* ${formattedDate}%0A*Hora:* ${time}%0A*Servicio:* ${service}%0A%0A*Mensaje:* ${message || 'Sin mensaje adicional'}`;
      
      // Open WhatsApp with the message
      window.open(`https://wa.me/56920449036?text=${whatsappMessage}`, '_blank');
      
      // Optional: Reset form
      bookingForm.reset();
    });
  }
</script>